import Color from 'color';
import { colors } from '@/core/themes/default/colors';

export const elevation = {
  M: `0 2px 5px ${Color(colors.basic.black).alpha(0.3)}`,
  L: `0 3px 10px ${Color(colors.basic.black).alpha(0.3)}`,
};
